import React from 'react';
import { Box, Typography } from '@mui/material';

export const TeamsTable: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Teams</Typography>
      <Typography>Here will be the list of teams...</Typography>
    </Box>
  );
};
