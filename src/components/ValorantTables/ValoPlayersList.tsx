import React from 'react';
import { Box, Typography } from '@mui/material';

export const ValoPlayersList: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Players</Typography>
      <Typography>Here will be the list of players...</Typography>
    </Box>
  );
};
